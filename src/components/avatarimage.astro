---

import { urlFor } from '../lib/sanity'

const { node } = Astro.props
const imageUrl = node?.asset ? urlFor(node.asset).width(300).url() : ''

---
<img class="avatar" src={imageUrl} alt={node?.alt || ''} />
